<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jodiakyulas.bloodbankers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.StartingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.AppointmentActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.BloodBankDetailsActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>


        <activity android:name=".activities.ChooseAppointmentTimingActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>

        <activity android:name=".activities.ConfirmAppointmentActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>

        <activity android:name=".activities.ViewCurrentAppointmentActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.ViewAppointmentHistoryActivity">
            <intent-filter>
                <action android:name="android.support.PARENT_ACTIVITY" />

            </intent-filter>
        </activity>
    </application>

</manifest>