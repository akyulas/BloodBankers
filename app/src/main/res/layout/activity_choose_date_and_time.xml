<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_height="match_parent" android:layout_width="match_parent">

    <EditText android:id="@+id/chosen_date"
        android:layout_height="wrap_content"
        android:layout_width="200dp"
        />

    <Button android:id="@+id/date_picker_button"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="SELECT DATE"
        android:layout_toRightOf="@id/chosen_date"
        android:layout_alignBottom="@id/chosen_date"
        />

    <EditText android:id="@+id/chosen_time"
        android:layout_height="wrap_content"
        android:layout_width="200dp"
        android:layout_below="@id/chosen_date"
        />

    <Button
        android:id="@+id/time_picker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/date_picker_button"
        android:layout_toRightOf="@id/chosen_time"
        android:text="SELECT TIME"
        android:layout_alignBottom="@id/chosen_time"/>

    <Button
        android:id="@+id/create_appointment"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_below="@+id/chosen_time"
        android:text="Create an appointment"
        />
</RelativeLayout>